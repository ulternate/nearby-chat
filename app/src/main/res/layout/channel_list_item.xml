<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/bg_message_item_received">

    <TextView
        android:id="@+id/channel_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_message_user_avatar"
        android:textStyle="bold"
        android:text="test"
        android:textColor="@color/textLight"
        />

    <TextView
        android:id="@+id/channel_topic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This is a channel topic"
        android:textColor="@color/textLight"
        android:layout_below="@+id/channel_title"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        />

    <ImageView
        android:id="@+id/channel_private"
        android:layout_width="@dimen/message_send_button"
        android:layout_height="@dimen/message_send_button"
        android:src="@drawable/ic_lock"
        android:layout_alignParentRight="true"
        />



</RelativeLayout>